import model from 'src/models/userModel'
import actions from './actions'
import mutations from './mutations'

const state = {
  userInfo: model
}

const getters = {
  
}

export default {
  state,
  getters,
  actions,
  mutations
}
